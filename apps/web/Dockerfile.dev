FROM node:22-alpine

WORKDIR /app

# package.jsonとpackage-lock.jsonをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install --legacy-peer-deps

# アプリケーションファイルをコピー
COPY . .

# 3000番ポートを開放
EXPOSE 3000

# 開発サーバーを起動（ホットリロード対応）
CMD ["npm", "run", "dev"]